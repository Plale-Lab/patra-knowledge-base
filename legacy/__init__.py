"""Suspended legacy code retained for archival/reference only.

The active backend is `rest_server/` (FastAPI + PostgreSQL). This package exists
solely to preserve historical Neo4j-era modules in a namespaced location.
"""

